apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-nfs-pvc
spec:
  volumeName: prometheus-nfs-pv
  accessModes:
  - ReadWriteMany
  resources:
     requests:
       storage: 10Gi

